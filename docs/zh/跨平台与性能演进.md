# LunaVox 跨平台重构与性能演进报告

> 日期：2026-04-13
> 分支：main（本次改动未提交）
> 目标：收敛 Windows / Linux / macOS 三端的底层差异，搭建可复现的基线度量体系，并基于实测数据给出后续优化路线。

---

## 1. 跨平台底层重构总结

此前的代码把平台差异以零散的 `#ifdef _WIN32` 分支散布在 `llama_wrapper.cpp`、`ort_provider_policy.cpp`、`main.cpp` 和 CMake 配置里。重构目标是：**把平台差异收敛到少量抽象点，让 macOS / Linux 的可工作面积与 Windows 对齐**。

### 1.1 动态库加载收敛（`src/llama_wrapper.cpp`）

- 新增 `DynLib` 结构体，统一封装 `LoadLibrary / dlopen`、`GetProcAddress / dlsym`、以及"查询已加载模块"的入口。
- 新增 `dynlib_try_open_in(dir, candidates...)`：使用 `std::filesystem::path` 拼接路径，依次尝试候选库名。
- 平台候选名表补齐 macOS：
  - `ggml-base`: `ggml-base.dll` / `libggml-base.so` / `libggml-base.dylib`
  - `llama`: `llama.dll` / `libllama.so` / `libllama.dylib`
- `ensure_loaded()` 重写为 `fs::u8path(lib_dir)` + 候选列表驱动，不再在函数体里再写 `#ifdef`。

**收益**：macOS 的 `.dylib` 首次进入可工作面，llama 后端路径解析走同一条代码路径，后续新增发行产物（例如 musl Linux）只需要追加一行候选名。

### 1.2 CoreML EP 落地（`src/ort_provider_policy.cpp`）

此前 macOS 的 CoreML 分支是占位。本次补齐：

```cpp
opts.AppendExecutionProvider("CoreMLExecutionProvider", {
    {"ModelFormat",              "MLProgram"},
    {"MLComputeUnits",           role == decoder ? "CPUAndGPU" : "ALL"},
    {"RequireStaticInputShapes", "0"},
    {"EnableOnSubgraphs",        "1"},
});
```

- decoder 走 `CPUAndGPU`，**主动避开 ANE**：Neural Engine 对动态 shape / 非 F16 输入限制多，decoder 是可变长图，打开 ANE 常会整段回落到 CPU 反而更慢。
- 其余角色保留 `ALL`，交给 CoreML 调度器决定。

**收益**：Apple Silicon 上 CoreML EP 具备真实执行路径；当实机验证可用后，无需再改代码即可切换到 `ALL`。

### 1.3 CMake 运行时库分发（`CMakeLists.txt`）

- `QWEN3_TTS_RUNTIME_DLLS` → `QWEN3_TTS_RUNTIME_BINS`，glob 模式同时匹配 `*.dll` / `*.so*` / `*.dylib`。
- 运行时库拷贝从 Windows-only 扩展到所有平台。
- rpath 策略分平台：
  - macOS: `@loader_path;@loader_path/../lib` + `MACOSX_RPATH ON`
  - Linux: `$ORIGIN;$ORIGIN/../lib`
- 在 `project()` 之前设置 `CMAKE_OSX_DEPLOYMENT_TARGET` 默认 11.0，让编译器选项从第一次配置就一致。

### 1.4 macOS 构建驱动（`src/lunavox/build/macos.py`）

- 检测宿主架构（`arm64` vs `x86_64`），显式传 `-DCMAKE_OSX_ARCHITECTURES=<arch>`，避免 Xcode 交叉目标让 CMake 自选出错误架构。
- 按架构选 Homebrew 前缀（`/opt/homebrew` 或 `/usr/local`）并注入 `PATH`。
- 环境变量覆盖：`LUNAVOX_OSX_ARCH`、`MACOSX_DEPLOYMENT_TARGET`。

### 1.5 CLI 入口统一（`src/main.cpp`）

- `collect_cli_args_utf8()` 整合为一个函数，内部用 `#ifdef` 分支处理 Windows `CommandLineToArgvW` + UTF-8 转换，POSIX 直接透传 argv。上层代码不再感知差异。
- 在 `tts.load_models()` 周围加了 `chrono` 计时，将 `t_load_ms` 写入 `--stats-json`。

### 1.6 `--stats-json` schema 调整

从数组升级为带顶层 `t_load_ms` 的对象：

```json
{
  "t_load_ms": 1714,
  "runs": [ { "run_id": 1, "timing_ms": {...}, "mem": {...}, ... }, ... ]
}
```

这样冷加载开销可以与每次合成的时间分开观测。

### 1.7 仍属"代码完成、机器未验证"的部分

必须诚实标注：**本次只在 Windows（DML + Vulkan llama 后端）上实测通过**。

- Linux：CMake / rpath / 候选库名已补齐，但未在真实机器跑通 CI。
- macOS：CoreML EP 代码可编译，但需要 Apple Silicon 机器实测 `MLComputeUnits` 的真实行为才能定稿。

这两项挂在后续路线图里。

---

## 2. 基线测量方案

### 2.1 测量工具（`tests/bench_baseline.py`）

一个独立的 Python 驱动脚本，**完全把 CLI 当黑盒**，不 import 任何 LunaVox 内部模块。避免测量代码自身干扰被测对象。

- 用 `subprocess.Popen` 启动 `build/qwen3-tts-cli`。
- 同步开一个 `ResourceSampler` 线程，以 **100 ms** 为间隔采集：
  - `psutil.Process.memory_info().rss`（进程物理内存）
  - `pynvml.nvmlDeviceGetMemoryInfo(handle).used`（整机 VRAM，用来和启动前基线做差）
- 子进程退出后，读取 `--stats-json` 获取 load / tokenize / encode / generate / decode 的毫秒分解。
- 每次运行写出 `raw_<tag>.json`、`stats_<tag>.json`、输出 wav，以及跨配置聚合的 `summary.json`。

### 2.2 矩阵

| tag | 模型目录 | 模式 | 说明 |
|---|---|---|---|
| `base_small__base` | models/base_small | base | 0.6B 模型，内置说话人 |
| `base_small__clone` | models/base_small | clone | 0.6B + 参考音克隆 |
| `base__base` | models/base | base | 1.7B 模型，内置说话人 |
| `base__clone` | models/base | clone | 1.7B + 参考音克隆 |

每个配置 `--repeat 2` 连续跑两次，用来观测"冷启动 → 热运行"落差。输入文本固定为一段约 13 秒的英文基准句。

### 2.3 运行环境

- OS：Windows 11 Pro (10.0.26200)
- 解码器 EP：`DmlExecutionProvider`
- llama 后端：Vulkan（`ggml-base.dll` + `llama.dll`）
- 测量时间：2026-04-13 11:30:54

---

## 3. 基线数据

结果目录：`tests/results/baseline_20260413_113054/`

### 3.1 聚合矩阵

| 配置 | t_load | run1 total | run2 total | RTF₁ | RTF₂ | RSS peak | VRAM delta | 音频时长 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| base_small · base  | 1714 ms | **7976 ms** | 2934 ms | 0.619 | 0.228 | 924 MB  | 1102 MB | 12.88 s |
| base_small · clone | 1436 ms | 3059 ms    | 2519 ms | 0.267 | 0.220 | 1033 MB | 1049 MB | 11.44 s |
| base · base        | 2170 ms | 5936 ms    | 5166 ms | 0.515 | 0.448 | 1712 MB | 1721 MB | 11.52 s |
| base · clone       | 1756 ms | 6172 ms    | 5554 ms | 0.491 | 0.442 | 1665 MB | 1758 MB | 12.56 s |

### 3.2 分阶段耗时（run1）

| 配置 | tokenize | encode | generate | decode |
|---|---:|---:|---:|---:|
| base_small · base  | 0 | 0   | 5954 | 2017 |
| base_small · clone | 0 | 198 | 1660 | 1399 |
| base · base        | 0 | 0   | 4384 | 1547 |
| base · clone       | 0 | 221 | 4637 | 1534 |

### 3.3 观察到的关键现象

1. **`base_small · base` 的冷/热落差异常巨大（7976 → 2934 ms，**2.7×**）**。对照 `base_small · clone` 的 run1 只要 3059 ms，说明 base 模式第一次合成时走了一条 clone 模式没走过的冷路径——最可能的嫌疑是**说话人嵌入/默认音色资源的首次展开 + DML decoder 首次编译**。生产用户每次 CLI 启动都会吃掉这 5 秒，是优先级最高的打击点。

2. **大模型 `base` 的冷/热落差反而不明显**（5936 → 5166 ms，只有 1.15×）。说明 1.7B 的 generate 阶段已经被采样步数主导，warmup 在总时长里占比被稀释。大模型的优化要回到"每步推理本身"。

3. **VRAM 占用几乎等于解码器权重大小**：0.6B 约 1.1 GB，1.7B 约 1.7 GB；和 RSS 峰值几乎 1:1 对应。意味着 **ONNX decoder session 是显存的绝对主角**，llama predictor 的 Vulkan 后端只贡献很小一部分。压缩显存要从 decoder session 下手。

4. **clone 模式相对 base 模式只多约 100–110 MB RSS**（0.6B: 924→1033；1.7B: 1712→1665 ≈ 持平）。speaker encoder 的常驻代价在整体里不算突出，不是瓶颈。

5. **t_load_ms 1.4–2.2 s**，小于冷合成 5–8 秒的时间。这确认了**冷启动慢的大头不在模型加载，而在第一次前向**——mmap 权重加载本身没问题，瓶颈在首次图编译 / arena 扩张 / KV cache 初分配。

6. **encode 耗时**（clone 模式下 198–221 ms）稳定且已经很短，暂不列为优化点。

7. **tokenize 在毫秒级以下**，可忽略。

---

## 4. 演进路线（优先级排序）

### P0：冷启动首帧打击（预计 2.7× 提速 on base_small）

**问题定位**：`base_small · base` 的 run1 比 run2 慢 5 秒以上，而 clone 模式 run1 却很快。差异点是 base 模式首次加载默认说话人 + 默认输入形状与 clone 不同，导致 DML session 对 decoder 的首次图编译发生在第一次合成里，而不是 `load_models()`。

**方案**：

- 在 `TTS::load_models()` 末尾执行一次"**空壳 warmup**"——用固定形状的零输入喂一次 decoder，把 DML / CoreML / CUDA 的 kernel 编译和 arena 扩张成本前置到"加载阶段"，让用户感知到的是更长的 load 而不是更长的首次合成。
- warmup 开销算到 `t_load_ms`，schema 里加一个 `warmup_ms` 字段方便后续对比。
- 对 clone 模式也做同样处理（针对 encoder / speaker embed）。

### P1：ONNX decoder 显存瘦身

**问题定位**：显存基本等于解码器权重，说明没有明显浪费；但两处可以挤：

1. **禁用 memory pattern** 对动态 shape 无收益，反而可能留下陈旧 arena。试在 `SessionOptions` 关掉 `enable_mem_pattern`，对比 RSS/VRAM 曲线。
2. **arena 策略调整**：改用 `kSameAsRequested` 而不是默认的 `kNextPowerOfTwo`，对偶发大尺寸输入更友好。
3. 评估 decoder 模型本身是否已经是 F16——如果还是 F32，F16 化会直接砍掉一半显存。

度量方法：在 bench_baseline 的 summary 里加一个 `vram_steady_mb`（取最后 1 秒的 VRAM 平均值），对比优化前后。

### P2：F16 codec 嵌入缓存的有界 LRU

codec 嵌入表如果走 F16 + mmap，冷路径没问题；但一旦有人跑长文本，命中率会抖。加一个**尺寸有界的 LRU**（比如 64 MB 上限），让常用 token 的 F16 展开结果常驻，既控制总内存又避免重复反量化。

### P3：llama predictor 的 KV cache 动态化

当前 `ctx_allocated` 固定 512，`ctx_cap` 2048。对短输入（本次测试 `ctx_required = 480`）浪费不大，但一旦要做流式或长文本合成，应该支持动态增长而不是一次申请到上限。

### P4：更细粒度的分阶段计时

当前 `timing_ms` 只有 tokenize / encode / generate / decode 四个桶。建议拆到：

- `generate` → `llama_eval_prompt` / `llama_eval_loop` / `talker_decode`
- `decode` → `codec_decode` / `pcm_overlap_add`

没有这层拆分时，"优化哪一步"的讨论会停留在猜。

### P5：macOS & Linux 实测闭环

- Apple Silicon 机器上跑一次 `lunavox build` + `tests/bench_baseline.py`，验证：
  1. `.dylib` 候选路径是否命中；
  2. CoreML `CPUAndGPU` 比 `ALL` 是否真的更快（或者相反——要用数据否定/确认）；
  3. rpath 在 `@loader_path` 下是否能解析 onnxruntime。
- Linux 至少覆盖一套 glibc 发行版（Ubuntu 22.04），确认 `$ORIGIN` rpath + `.so.*` glob 正确。

### P6：GUI 侧的冷启动感知

P0 做完以后，GUI 应该把"模型加载"进度条延伸覆盖首次 warmup。用户点 Load 之后不应该还有一次看不见的 5 秒卡顿。

---

## 5. 小结

- **跨平台**：把散落的 `#ifdef` 收敛到了 `DynLib` 抽象、候选库名表和 CMake rpath 分支里。macOS / Linux 的代码路径从"占位"升级到"可编译并预期可工作"，但仍需真实机器闭环。
- **测量体系**：`tests/bench_baseline.py` 是后续所有性能工作的度量基准；它把 CLI 当黑盒、按 100 ms 采样 RSS + VRAM，任何优化都应该回来这张表上给出前后对比。
- **数据发现**：0.6B 模型的冷启动 2.7× 落差是最先要啃的骨头，解决它对用户体验的影响比任何微观 kernel 优化都明显。显存被 ONNX decoder 主导，是第二战场。clone / encode / tokenize 都不是瓶颈，不要在这上面浪费预算。
- **下一步**：按 P0 → P1 顺序做，并在 `baseline_<timestamp>` 目录里保留每次迭代的数据，保证每一次"快了"都是有据可查的，而不是凭感觉。
