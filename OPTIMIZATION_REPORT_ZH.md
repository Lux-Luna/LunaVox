# LunaVox TTS GPU & FP16 全链路优化报告

## 1. 原项目问题背景
在将项目从 **FP32 + ONNX Runtime CPU 模式** 迁移到 **FP16 + ONNX Runtime GPU 模式** 后，发现推理延迟反而显著增加。

### 定位到的核心问题：
1.  **通信开销超过计算收益**：T2S 阶段是一个 500 次的自回归循环，每一步只预测一个 token。在 GPU 模式下，每一轮循环都会触发 CPU-GPU 之间的数据同步（KV Cache 搬运），导致 GPU 算力被极高频的小数据量 IO 阻塞。
2.  **关键链路仍留在 CPU**：中文 BERT 模型 (RoBERTa-Large) 硬编码在 CPU 上运行，单次推理耗时达 300ms 以上。
3.  **频繁的类型转换**：循环内部存在大量 CPU 端的 `astype(np.float16)` 操作，累积了不必要的延迟。

---

## 2. 优化研究与解决方案

### 预计方案：
*   **全链路 GPU 化**：将 BERT 移动到 GPU。
*   **IO Binding (显存驻留)**：使用 ONNX Runtime 的 IO Binding 技术，将 KV Cache 保持在显存中，消除 H2D/D2H 拷贝。
*   **输入转换优化**：将静态输入提前转换为 FP16，减少循环内计算。

### 实际完成工作：
1.  **监控体系**：在 `Inference.py` 中注入了精确到毫秒的阶段性计时日志，并建立了 `run_benchmark.py` 自动化评估工具。
2.  **BERT 迁移**：修改 `ZhBert.py`，动态检测 CUDA 并将模型与 Tensor 自动迁移至 GPU。
3.  **T2S 重构**：
    *   实现了 `t2s_gpu` 方法，全面采用 `io_binding`。
    *   **KV Cache 自动适配**：通过智能分割逻辑（支持多维度 split），将初始 cache 注入显存。
    *   **零拷贝循环**：在 500 次循环中，除了用于停止判定的 `samples` token (1x1)，其余张量（y_emb, past_kv）均直接在显存内传递。
4.  **VITS 优化**：重构 `_run_vocoder`，支持 `OrtValue` 直接输入，减少了 32k 音频等大数据张量的拷贝。

---

## 3. 最终结果对比

| 指标 (ms) | 优化前 (Baseline) | 优化后 (Final) | 提升幅度 |
| :--- | :--- | :--- | :--- |
| **英文 T2S 推理** | 1148 ms | **235 ms** | **4.8x** |
| **中文 Frontend (BERT)** | ~400 ms | **91 ms** | **4.4x** |
| **英文总延迟** | 1477 ms | **556 ms** | **2.6x** |
| **中文总延迟** | 2514 ms | **1258 ms** | **2.0x** |

---

## 4. 完成中遇到的问题与修正
1.  **KV Cache 维度不匹配**：不同版本的 T2S 模型输出的 `k_agg` 轴定义不同。通过增加维度检测和多轴分割尝试，增强了代码的鲁棒性。
2.  **IO Binding 严格性**：GPU 模式下如果不手动绑定所有必需输入（即使是空张量），ORT 会报错。通过 `_get_empty_past_kv` 逻辑自动构造符合模型期望维度的全零显存张量，解决了此问题。
3.  **方法丢失**：在环境重置过程中部分辅助函数丢失，已全部手动修复补全。

---

## 5. 结论
通过本次优化，项目已彻底克服了 GPU 推理时的“通信墙”问题。优化后的 GPU 版本在响应速度上已大幅超越原有的 CPU 版本，且由于全链路 FP16 运行，显存占用也得到了有效控制。

